"""Version del widget. Se usa para chequear actualizaciones contra GitHub
Releases (ver `update_checker.py`) -- subir un release nuevo se etiqueta
como `vX.Y.Z` y debe coincidir con lo que se sube aca cuando se hace un
build nuevo.
"""

VERSION = "1.1.0"
